[package]
name = "mingscribe"
version = "0.4.0"
description = "MingScribe 电子书阅读器（桌面版）"
authors = ["MingScribe"]
edition = "2021"
rust-version = "1.70"

[lib]
name = "mingscribe_lib"
crate-type = ["staticlib", "cdylib", "rlib"]

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }
base64 = "0.22"
